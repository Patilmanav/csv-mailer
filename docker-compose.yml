version: '3.8'

services:
  csv-mailer:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./templates:/app/templates
    env_file:
      - .env
    restart: unless-stopped
